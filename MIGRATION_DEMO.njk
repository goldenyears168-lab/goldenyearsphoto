---
layout: base-layout.njk
title: 遷移示範頁面 | Golden Years Photo
description: 展示如何將現有代碼遷移到使用標準化元件
---

{% from "macros/heading.njk" import heading %}
{% from "macros/container.njk" import container %}
{% from "macros/button.njk" import button %}
{% from "macros/card.njk" import card %}
{% from "macros/grid.njk" import grid %}
{% from "macros/alert.njk" import alert %}

<div class="min-h-screen bg-sand-50 py-16">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    {{ heading(1, "代碼遷移示範", "4xl", "mb-12 text-center") }}
    
    <!-- 遷移範例 1: 按鈕 -->
    <section class="mb-16">
      {{ heading(2, "範例 1: 按鈕遷移", "3xl", "mb-8") }}
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <!-- 之前 -->
        <div class="bento-card bento-card-default p-6">
          <h3 class="text-lg font-bold text-trust-900 mb-4">之前（硬編碼）</h3>
          <div class="space-y-4">
            <pre class="text-xs bg-sand-100 p-4 rounded overflow-x-auto"><code>&lt;a href="/booking/" 
  class="bg-white text-slate-900 px-8 py-4 rounded-full font-bold border-2 border-trust-950 hover:bg-sand-50 transition-colors"&gt;
  預約拍攝
&lt;/a&gt;</code></pre>
            <div class="pt-4 border-t border-sand-200">
              <a href="/booking/" class="bg-white text-slate-900 px-8 py-4 rounded-full font-bold border-2 border-trust-950 hover:bg-sand-50 transition-colors inline-block">
                預約拍攝
              </a>
            </div>
          </div>
        </div>
        
        <!-- 之後 -->
        <div class="bento-card bento-card-default p-6">
          <h3 class="text-lg font-bold text-trust-900 mb-4">之後（使用 Macro）</h3>
          <div class="space-y-4">
            <pre class="text-xs bg-sand-100 p-4 rounded overflow-x-auto"><code>{% from "macros/button.njk" import button %}
{{ button("預約拍攝", "/booking/", "secondary", "lg") }}</code></pre>
            <div class="pt-4 border-t border-sand-200">
              {{ button("預約拍攝", "/booking/", "secondary", "lg") }}
            </div>
          </div>
        </div>
      </div>
    </section>
    
    <!-- 遷移範例 2: 卡片 -->
    <section class="mb-16">
      {{ heading(2, "範例 2: 卡片遷移", "3xl", "mb-8") }}
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <!-- 之前 -->
        <div class="bento-card bento-card-default p-6">
          <h3 class="text-lg font-bold text-trust-900 mb-4">之前（硬編碼）</h3>
          <div class="space-y-4">
            <pre class="text-xs bg-sand-100 p-4 rounded overflow-x-auto"><code>&lt;div class="bento-card bg-sand-50 rounded-2xl mb-16"&gt;
  &lt;h3 class="text-2xl font-bold mb-4"&gt;標題&lt;/h3&gt;
  &lt;p class="text-slate-600"&gt;內容&lt;/p&gt;
&lt;/div&gt;</code></pre>
            <div class="pt-4 border-t border-sand-200">
              <div class="bento-card bg-sand-50 rounded-2xl p-4">
                <h3 class="text-lg font-bold mb-2">標題</h3>
                <p class="text-sm text-slate-600">內容</p>
              </div>
            </div>
          </div>
        </div>
        
        <!-- 之後 -->
        <div class="bento-card bento-card-default p-6">
          <h3 class="text-lg font-bold text-trust-900 mb-4">之後（使用 Macro）</h3>
          <div class="space-y-4">
            <pre class="text-xs bg-sand-100 p-4 rounded overflow-x-auto"><code>{% from "macros/card.njk" import card %}
{{ card("sand", "mb-16", "&lt;h3 class=\"text-2xl font-bold mb-4\"&gt;標題&lt;/h3&gt;&lt;p class=\"text-slate-600\"&gt;內容&lt;/p&gt;") }}</code></pre>
            <div class="pt-4 border-t border-sand-200">
              {{ card("sand", "", "<h3 class=\"text-lg font-bold mb-2\">標題</h3><p class=\"text-sm text-slate-600\">內容</p>") }}
            </div>
          </div>
        </div>
      </div>
    </section>
    
    <!-- 遷移範例 3: 表單 -->
    <section class="mb-16">
      {{ heading(2, "範例 3: 表單遷移", "3xl", "mb-8") }}
      
      <div class="bento-card bento-card-default p-8">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          <!-- 之前 -->
          <div>
            <h3 class="text-lg font-bold text-trust-900 mb-4">之前（硬編碼）</h3>
            <pre class="text-xs bg-sand-100 p-4 rounded overflow-x-auto"><code>&lt;div class="mb-4"&gt;
  &lt;label for="name" class="block text-sm font-medium mb-2"&gt;姓名&lt;/label&gt;
  &lt;input type="text" id="name" name="name" 
    class="w-full px-4 py-3 border border-sand-200 rounded-lg" required&gt;
&lt;/div&gt;</code></pre>
          </div>
          
          <!-- 之後 -->
          <div>
            <h3 class="text-lg font-bold text-trust-900 mb-4">之後（使用 Macro）</h3>
            <pre class="text-xs bg-sand-100 p-4 rounded overflow-x-auto"><code>{% from "macros/input.njk" import input %}
{{ input("text", "name", "user-name", "姓名", "請輸入姓名", "", true) }}</code></pre>
          </div>
        </div>
        
        <div class="mt-8 pt-8 border-t border-sand-200">
          <h3 class="text-lg font-bold text-trust-900 mb-4">實際效果對比</h3>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <div>
              <label for="demo-name-old" class="block text-sm font-medium mb-2">姓名（舊方式）</label>
              <input type="text" id="demo-name-old" name="name-old" class="w-full px-4 py-3 border border-sand-200 rounded-lg" required>
            </div>
            <div>
              {% from "macros/input.njk" import input %}
              {{ input("text", "demo-name-new", "demo-name-new", "姓名（新方式）", "請輸入姓名", "", true) }}
            </div>
          </div>
        </div>
      </div>
    </section>
    
    <!-- 遷移優勢 -->
    <section class="mb-16">
      {{ heading(2, "遷移優勢", "3xl", "mb-8") }}
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {{ card("default", "", "<h3 class=\"text-xl font-bold mb-2\">代碼簡潔</h3><p class=\"text-slate-600\">從多行 HTML 減少到單行 macro，代碼更易讀</p>") }}
        {{ card("default", "", "<h3 class=\"text-xl font-bold mb-2\">統一樣式</h3><p class=\"text-slate-600\">所有元件使用統一的設計系統，確保一致性</p>") }}
        {{ card("default", "", "<h3 class=\"text-xl font-bold mb-2\">易於維護</h3><p class=\"text-slate-600\">集中管理元件樣式，更新一處即可影響全局</p>") }}
      </div>
    </section>
    
    <!-- 遷移步驟 -->
    <section class="mb-16">
      {{ heading(2, "遷移步驟", "3xl", "mb-8") }}
      
      <div class="bento-card bento-card-default p-8">
        <ol class="space-y-4 list-decimal list-inside">
          <li class="text-trust-900">
            <strong>導入 Macro</strong>
            <pre class="mt-2 text-xs bg-sand-100 p-3 rounded"><code>{% from "macros/button.njk" import button %}</code></pre>
          </li>
          <li class="text-trust-900">
            <strong>替換代碼</strong>
            <pre class="mt-2 text-xs bg-sand-100 p-3 rounded"><code>{{ button("文字", "/link/", "primary", "lg") }}</code></pre>
          </li>
          <li class="text-trust-900">
            <strong>測試驗證</strong>
            <p class="mt-2 text-slate-600">確保功能和樣式正常</p>
          </li>
          <li class="text-trust-900">
            <strong>提交更改</strong>
            <p class="mt-2 text-slate-600">完成遷移後提交代碼</p>
          </li>
        </ol>
      </div>
    </section>
    
    <!-- 相關資源 -->
    <section>
      {{ heading(2, "相關資源", "3xl", "mb-8") }}
      
      <div class="bento-card bento-card-default p-8">
        <ul class="space-y-3">
          <li>
            <a href="/components-showcase/" class="text-trust-600 hover:text-trust-800 underline">
              元件展示頁面
            </a>
            - 查看所有可用元件
          </li>
          <li>
            <a href="https://github.com/your-repo/blob/main/src/_includes/macros/README.md" class="text-trust-600 hover:text-trust-800 underline">
              元件庫文檔
            </a>
            - 完整的 API 文檔和使用指南
          </li>
          <li>
            <span class="text-trust-600">MIGRATION_SUGGESTIONS.md</span>
            - 自動生成的遷移建議
          </li>
          <li>
            <span class="text-trust-600">MIGRATION_EXAMPLE.md</span>
            - 遷移範例和最佳實踐
          </li>
        </ul>
      </div>
    </section>
  </div>
</div>

