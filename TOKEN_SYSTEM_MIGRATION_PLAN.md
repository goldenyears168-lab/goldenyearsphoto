# CSS Variables 為主的 Token 系統遷移計劃

## 📊 當前狀態

### 現有 Token 定義位置
1. **CSS Variables** (`src/assets/css/main.css`): 已定義完整的設計 token
2. **Tailwind Config** (`tailwind.config.js`): 重複定義了相同的顏色值

### 問題
- Token 定義分散在兩個地方
- 維護成本高（需要同步更新兩處）
- 容易出現不一致

## 🎯 遷移目標

建立以 **CSS Variables 為單一來源**的設計 token 系統，Tailwind 從 CSS Variables 讀取值。

## 📋 遷移步驟

### Phase 1: 擴展 CSS Variables（已完成 ✅）
- ✅ 定義所有顏色 token
- ✅ 定義字體 token
- ✅ 定義間距 token
- ✅ 定義圓角 token
- ✅ 定義陰影 token

### Phase 2: 更新 Tailwind Config（進行中）
將 `tailwind.config.js` 中的硬編碼值改為從 CSS Variables 讀取。

**當前方式**:
```javascript
colors: {
  'trust': {
    50: '#F0F4FF',  // 硬編碼
    100: '#E0E7FF',
    // ...
  }
}
```

**目標方式**:
```javascript
colors: {
  'trust': {
    50: 'var(--color-trust-50)',
    100: 'var(--color-trust-100)',
    // ...
  }
}
```

### Phase 3: 驗證與測試
- 確保所有 Tailwind 類名正常工作
- 確保 CSS Variables 在所有瀏覽器中正常運作
- 檢查是否有遺漏的 token

### Phase 4: 文檔化
- 更新設計系統文檔
- 創建 token 使用指南
- 標記 deprecated token

## 🔧 實施細節

### 1. Tailwind Config 更新

需要更新的部分：
- `colors` - 所有顏色定義
- `fontSize` - 字體大小
- `spacing` - 間距值
- `borderRadius` - 圓角值
- `boxShadow` - 陰影值

### 2. CSS Variables 結構

保持當前的結構，但確保：
- 所有 token 都有對應的 CSS Variable
- 命名一致且語義化
- 有清晰的註釋說明用途

### 3. 向後兼容

- 保留現有的 CSS Variables 定義
- 保留 Tailwind 類名（即使內部實現改變）
- 逐步遷移，不破壞現有功能

## ✅ 驗證清單

- [ ] 所有顏色 token 在 CSS Variables 中定義
- [ ] Tailwind config 從 CSS Variables 讀取
- [ ] 所有頁面正常顯示
- [ ] 沒有視覺回歸
- [ ] 文檔已更新

## 📝 注意事項

1. **瀏覽器兼容性**: CSS Variables 在現代瀏覽器中支持良好，但需要確認目標瀏覽器
2. **構建工具**: 確保構建工具（如 PostCSS）正確處理 CSS Variables
3. **性能**: CSS Variables 的性能影響很小，但需要監控

---

**創建日期**: 2025-12-14
**狀態**: 規劃中

